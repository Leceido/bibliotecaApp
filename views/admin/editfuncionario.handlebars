{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Editar conta Funcionario</h3>

<div class="card">
    <div class="card-body">
        <form action="/admin/funcionarios/edit" method="post">
        <input type="hidden" name="id" value="{{funcionario._id}}">
            <label for="nome">Nome: </label>
                <input type="text" name="nome" id="" class="form-control" value="{{funcionario.nome}}" required>
            <label for="email">Email: </label>
                <input type="email" name="email" id="" class="form-control" value="{{funcionario.email}}" required>
            <label for="senha">Senha: </label>
                <input type="password" name="senha" id="" class="form-control" placeholder="Digite um nova senha!" required>
            <button type="submit" class="btn btn-primary mt-2">Editar conta</button>
        </form>
    </div>
</div>